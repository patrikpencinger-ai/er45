# OS / editor cruft
Thumbs.db
Desktop.ini
.DS_Store
*~

# Local Claude session state (keep serve.ps1, ignore local settings)
.claude/settings.local.json

# Do NOT commit the original-site backup if it ever lands here.
# (It lives at C:\Users\patri\OneDrive\Backup\ER45 — 1.6 GB, read-only source.)
# NB: `archive/` is NOT ignored — that's the archive.er45.com subdomain site
# (reconstructed JSON + flyer thumbnails). Raw DB/dump/zip files stay ignored:
*.zip
*.mdb
*.dump
*.dump.gz
