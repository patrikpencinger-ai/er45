# OS / editor cruft
Thumbs.db
Desktop.ini
.DS_Store
*~

# Local Claude session state (keep serve.ps1, ignore local settings)
.claude/settings.local.json

# Do NOT commit the original-site archive backup if it ever lands here.
# (It lives at C:\Users\patri\OneDrive\Backup\ER45 — 1.6 GB, read-only source.)
archive/
*.zip
*.mdb
*.dump
